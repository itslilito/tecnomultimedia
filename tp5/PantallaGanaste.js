function pantallaGanaste(){
  background(100,100,255);
  
  fill(255);
  textAlign(CENTER, CENTER);
  textSize(76);
  text("¡Ganaste!",500,250);
  
  textAlign(CENTER, CENTER);
  textSize(24);
  text("Presioná la tecla ENTER para volver al inicio",500,441);
}
